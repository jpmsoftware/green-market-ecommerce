<section class="populares">
  <h2>MÁS POPULARES</h2>

  <div class="products-container">
    <% for (var i=0; i < populares.populares.length; i++) { %>
      <div class="card">
        <div class="thumb-container">
          <img src="/data/thumbs/<%= populares.populares[i].imagen %>" alt="thumb" class="product-thumb">
        </div>
        <div class="container">
          <h4 class="product-name"><%= populares.populares[i].nombre %></h4>
          <p class="product-description"><%= populares.populares[i].descripcion %></p>
          <p class="product-price">$ <%= populares.populares[i].precio %>
          </p>
        </div>
      </div>
      <% } %>
  </div>
</section>