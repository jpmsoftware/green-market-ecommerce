<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <link rel="stylesheet" href="/css/animations.css">
  <link rel="stylesheet" href="/css/responsive.css">
  <link rel="icon" href="/img/favicon.png">
  <title>Green Market</title>
</head>
<body>
  <!------------------------------HEADER------------------------------->
  <%- include('../partials/pre-header') %>
  <%- include('../partials/header') %>

  <!------------------------------PRODUCTS----------------------------->
  <section class="section-category">
    <div class="heading">
      <h2>Categoría</h2>
      <hr>
    </div>

    <p class="message" id="message"></p>

    <div class="products">
      <% products.map((product)=> { %>
        <div class="card">
          <div class="thumb-container">
            <img src="/data/thumbs/small/<%= product.img %>" class="product-image" alt="image">
          </div>
          <div class="info">
            <h4 class="product-name"><%= product.name %></h4>
            <span class="product-description" style="display: none"></span>
            <p class="product-price">$ <%= product.price %></p>
            <button class="button-add">Agregar al carrito</button>
          </div>
        </div>
        <% }) %>
      </div>
  </section>

  <!-----------------------------FOOTER------------------------------->
  <%- include('../partials/footer') %>

  <!------------------------------OTHER------------------------------->
  <%- include('../partials/modal') %>
  <%- include ('../partials/msg') %>
  <%- include('../partials/loading') %>
  <%- include('../partials/login') %>
  <div class="mask" id="mask"></div>

  <!-----------------------------SCRIPTS------------------------------>
  <script src="/js/main.js"></script>
  <script>
    let message = document.getElementById('message');
    let cardCount = document.querySelectorAll('.card').length;

    cardCount < 1
      ? message.innerHTML = `No se encontraron productos`
      : message.innerHTML = `Se encontraron <b>${cardCount} productos</b>`;

    message.classList.toggle('visible');
  </script>
</body>
</html>